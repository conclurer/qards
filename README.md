# qards

```
$ npm install -g cordova ionic
$ git clone https://github.com/conclurer/qards
$ git clone https://github.com/conclurer/qards_lib qards-lib
$ cd qards-lib && npm install && npm build
$ cd ../qards && npm install && cordova prepare && ionic cordova run
```
